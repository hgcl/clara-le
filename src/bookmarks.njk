---
title: Bookmarks
description: My collection of bookmarks
icon: /public/img/meta/drawing-list.svg
pagination:
    data: "bookmarkData"
    size: 1
---

{% from 'macro/list-filter-selector.njk' import listFilterSelector %}
{% extends "layout/base.njk" %} 

{% block head %}
<style>
  .intro { 
    margin-bottom: 3rem;
  }
  #random-link {
    margin: 0 auto;
    display: block;
  }
</style>
{% endblock %}

{% block article %}
<article class="center h-entry">
  <div class="e-content">
  {% include 'post-header.njk' %}
    <p class="intro">
      My collection of bookmarks of specific blog posts, articles, essays — spanning the web. 
    </p>

    <!-- POST FILTER SELECTOR -->
    {{ listFilterSelector('category', ['Accessibility','Blog','Coding','Cooking','Gaming','Web']) }}

    <!-- LINK LIST -->
      <ul
        class="nobullet post-rows-with-label filtered-list">
        {% for item in pagination.pages | reverse %}
          <li data-tag="{{ item.tags }}">
            <time class="label dt-duration" datetime="{{ item.date }}">{{ item.date | formatDate("short") }}</time>
            <span class="details">
              <a href="{{ item.url }}">{{ item.title }}</a>
              <span class="subtle">{{ item.description }}</span>
                {% set divider = joiner(" / ") %}
                {% if item.tags %}
                {%- for tag in item.tags -%}
                  {{ divider() }}<a class="chip" href="/bookmarks/?q={{ tag }}">{{ tag }}</a>
                {%- endfor -%}
                {% endif %}
            </span>
          </li>
        {% endfor %}
      </ul>
  </div>
</article>
{% endblock %}

{% block script %}
  {% include 'filter-notes-script.njk' %}
{% endblock %}