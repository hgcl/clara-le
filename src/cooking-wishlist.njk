---
title: Cooking wishlist
description: Recipes I want to
icon: /public/img/meta/drawing-list.svg
pagination:
    data: "cookingWishlistData"
    size: 1
---

{% from 'macro/list-filter-selector.njk' import listFilterSelector %}
{% extends "layout/base.njk" %} 

{% block head %}
<style>
  .intro { 
    margin-bottom: 3rem;
  }
  #random-link {
    margin: 0 auto;
    display: block;
  }
</style>
{% endblock %}

{% block article %}
<article class="center h-entry">
  <div class="e-content">
  {% include 'post-header.njk' %}
    <p class="intro">
      My forever growing list of recipes I want to try
    </p>

    <!-- POST FILTER SELECTOR -->
    {{ listFilterSelector('category', ['Main','Dessert','Basic','Snack','Drink','Preserve'], "tried", ["Tried","Wishlist"]) }}

    <!-- LINK LIST -->
      <ul
        class="nobullet post-rows-with-label filtered-list">
        {% for item in pagination.pages | reverse %}
          <li data-tag="{{ item.tags }}" data-tag-two="{% if item.tried %}tried{% else %}wishlist{% endif %}">
            <time class="label dt-duration" datetime="{{ item.date }}">{{ item.date | formatDate("short") }}</time>
            <span class="details">
              <a href="{{ item.url }}">{{ item.title }}</a>
              <span class="subtle">Found on {{ item.domain }}</span>
                {% if item.tags %}
                  <a class="chip" href="/cooking-wishlist/?q={{ item.tags }}">{{ item.tags }}</a>
                {% endif %} 
                {% if item.tried %}
                <a class="chip" href="/cooking-wishlist/?r=tried">Tried</a>{% endif %}
            </span>
          </li>
        {% endfor %}
      </ul>
  </div>
</article>
{% endblock %}

{% block script %}
  {% include 'filter-notes-script.njk' %}
{% endblock %}